import {page} from "../common.js";
export function ConLogistica(){ return page('Logística',`<div class="card">Planificación de rutas / despachos.</div>`); }
export function ConApp(){ return page('Conductores (App/GPS)',`<div class="card">App móvil + tracking — WIP.</div>`); }
