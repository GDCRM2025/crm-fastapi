import {page} from "../common.js";
export function FinGastos(){ return page('Registro de Gastos',`<div class="card">Formulario + listado.</div>`); }
export function FinIngresos(){ return page('Registro de Ingresos',`<div class="card">Formulario + listado.</div>`); }
export function FinPL(){ return page('P&L',`<div class="card">Estado de resultados.</div>`); }
export function FinPlanCuentas(){ return page('Plan de Cuentas',`<div class="card">ABM de cuentas contables.</div>`); }
