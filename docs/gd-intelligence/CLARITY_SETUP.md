# Microsoft Clarity setup

Registrar por sitio `clarity_project_id`, `dashboard_url` y `enabled`. El CRM mostrará un enlace al dashboard oficial. No copiará grabaciones ni mapas de calor a PostgreSQL. El script oficial se instala en cada sitio sólo después de validar consentimiento/cookies, CSP y dominio correcto.
