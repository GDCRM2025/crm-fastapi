# Matriz de permisos

La visibilidad del menú no sustituye autorización backend.

| ID | Función | Permiso explícito | Gate |
|---|---|---|---|
| system_notifs | Alertas | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| leads_ver | Ver Leads | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| leads_ver | Ver Leads | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| leads_fil | Filtrar Leads | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| crm360 | Comercial 360 | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| chk_hoy | Eventos (día) | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| encuestas_eventos | Encuestas post-evento | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| historial | Historial | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| rep_total | Ir a Reportes | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| gdi_overview | Dashboard web | web_intelligence_view | Backend y UI deben validarse conjuntamente |
| gdi_sites | Sitios | web_intelligence_view | Backend y UI deben validarse conjuntamente |
| gdi_integrations | Integration Center | web_intelligence_view | Backend y UI deben validarse conjuntamente |
| gdi_health | Site Health | web_intelligence_view | Backend y UI deben validarse conjuntamente |
| gdi_utm | Constructor UTM | web_intelligence_campaigns | Backend y UI deben validarse conjuntamente |
| gdi_paid_media | Paid Media | paid_media_view | Backend y UI deben validarse conjuntamente |
| gdi_permissions | Permisos GD | system_users_manage | Backend y UI deben validarse conjuntamente |
| emkt_email | Email marketing | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_rec | Recetas | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_mice | Mice and Place | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_ruta | Ruta | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_ma_cat | Categorias Maquinaria | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_ma_inv | Inventario Maquinaria | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_ma_ficha | Ficha Maquinaria | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_ca_ficha | Ficha Camiones | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_ca_ent | Entrega de Camiones | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_ca_dev | Devolucion de Camiones | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_chk_ev | Checklist eventos | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| inv_tomar | Tomar inventario | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| inv_stock | Stock ingredientes | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| inv_class | Clasificación MICE/OPS | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| inv_cat | Categorías | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| inv_uni | Unidades | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| inv_prov | Proveedores | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| inv_mov | Movimiento de Inventario | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| pl | P&L | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| gast | Cargar Gastos | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| plan_cuentas | Plan de Cuentas | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_gps | Conectar GPS | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_vruta | Ver ruta (actual) | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_cal | Calendario | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_menu_cam | Menu Camaleón | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_menu_gou | Menu Gourmet | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_menu_exp | Menu Express | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_menu_del | Menu Del Sabor | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_menu_pet | Menu Petras | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_menu_mf | Menu Mas Flow | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_uni | Universidad GD | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| op_vruta2 | Ver Ruta (próx.) | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| rrhh_hub | RRHH | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| rrhh_mark_plan | Turnos (planificador) | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| rrhh_turnos_visor | Mi turno (teórico vs real) | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| tool_gmail | Correo | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| tool_ig | Instagram | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| tool_wapp | WhatsApp | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| tool_calc | Calculadora | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_users | Usuarios | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_integrations | Integraciones | web_intelligence_configure | Backend y UI deben validarse conjuntamente |
| set_marcas | Marcas | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_prod | Productos (venta) | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_commissions_config | Configuración comisiones | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_com | Comunas | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_tc | Tipos Cliente | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_platforms | Plataformas / origen de leads | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_el | Estados Lead | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_roles | Roles | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_permissions | Permisos | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_features | Funcionamiento de módulos | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_whatsapp_coexistence | Coexistencia WhatsApp | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_audit | Auditoría | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_notify_email | Notificaciones (correo) | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_metas | Metas ventas | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
| set_bak | Backups | Controlado por PERMISSIONS del panel | Backend y UI deben validarse conjuntamente |
